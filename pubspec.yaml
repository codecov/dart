name: dart_codecov_generator
description: >
  Generates HTML lcov coverage reports for dart projects.(UNIX only)
  relies on:
  - coverage.dart (`pub global activate coverage`),
  - genhtml (part of lcov),
  - lsof (standard unix utility)
dependencies:
  args: any
dev_dependencies:
executables:
  generate_coverage:
